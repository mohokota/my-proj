<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {margin: 0; padding: 0;}
        body { 
            font-family: 'Inter', sans-serif; font-size: 14px;
        }
        .box-blog {
            max-width: 780px;
            margin: 50px auto;
            text-align: center;
        }
        .box-msg  {
            display: flex;
            flex-direction: column;
            gap: 10px;
            margin-top: 20px;
        }
        .box-msg > input, textarea {
            font-family: inherit; font-size: 12px;
            padding: 8px;
        }
        textarea#msg {
            white-space: pre-wrap;
            word-wrap: break-word;
            word-break: break-all;
            height: 60px;
        }
        #btn-check {
            background-color: #000000;
            width: 780px;
            padding: 10px;
            border: none;
            border-radius: 5px;
            align-self: center;
            color: #808080;
        }
        #btn-check:hover {
            background-color: #a9a9a9;
            color: #f5e6e6;
            cursor: pointer;
        }
        @media (min-width:541px) and (max-width: 768px) {
            .box-blog {max-width: 460px}
            #btn-check {width: 460px}
        } 
        @media (max-width: 540px) { 
            .box-blog {max-width: 368px}
            #btn-check {width: 368px}
        }
    </style>
</head>
<body>
    
    <section class="box-blog">
        <h1>Message</h1>
        <% if(locals.blog) { %>
        <form action="/modblg/<%=blog.key%>" method="post" class="box-msg">
            <input type="text" name="title" id="title" placeholder="title.." required/>
            <textarea type="text" name="content" id="msg" row="3" wrap="soft" placeholder="message.."> </textarea>
            <input type="text" name="author" id="author" placeholder="author.." required/>
            <button type="submit" id="btn-check">
                <span><b> Update blog </b></span> 
            </button>
        </form>
        <% } %>
    </section>   

    <script>
        document.querySelector("input").style.padding = "5px";
    </script>


</body>
</html>

